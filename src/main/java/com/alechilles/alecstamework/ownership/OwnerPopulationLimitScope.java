package com.alechilles.alecstamework.ownership;

/** Defines which committed and pending owner count a configured limit constrains. */
public enum OwnerPopulationLimitScope {
    GLOBAL,
    PER_WORLD
}
