package com.alechilles.alecstamework.persistence.projection;

/** Replacement outbox retention policy; compaction remains disabled until separately proven. */
public enum ProjectionCompactionPolicy {
    DISABLED
}
