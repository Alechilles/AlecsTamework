package com.alechilles.alecstamework.persistence.control;

/** Canonical domain owned or coordinated by a persistence feature descriptor. */
public enum PersistenceFeatureDomain {
    IDENTITY,
    LIFECYCLE,
    POPULATION,
    COMMAND,
    CAPTURE,
    DORMANT,
    COOP,
    COMPENSATION,
    EXTENSION
}
